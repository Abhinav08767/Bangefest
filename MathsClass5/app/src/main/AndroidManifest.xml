<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.s.mathsclass5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher1_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EstimationRounding" />
        <activity android:name=".System" />
        <activity android:name=".Geometry" />
        <activity android:name=".Perimetre" />
        <activity android:name=".Area" />
        <activity android:name=".df" />
        <activity android:name=".FirstPage"></activity>
    </application>

</manifest>